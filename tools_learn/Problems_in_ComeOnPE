1. 执行exe文件时总是提示runtime-dector没找到合适的intepreter。因为安装了mono，所以对于exe有相应的解释器，被binfmt_misc选中，但是解释又失败了，所以出现这样的错误。于是用apt-get remove命令卸载了mono，再插入自己的binfmt_pe模块，执行exe，成功调用了!
2. 装载ELF文件的大致过程：根据文件头将各区映像装入，并将解释器也装入，然后把EIP指向ld-linux的入口地址，然后由它把共享库装入。（当文件中的所有库为静态库时不需要解释器，但现在一般都是动态库）
3. 总结两种动态连接库函数的调用过程，简明流程如下：
	懒连接：
	调用点->PLTn->PLT0->GOT0->_dl_runtime_resolve()->fixup()->被调用函数入口->返回调用点。
	在完成了动态调用之后：
	调用点->PLTn->GOTn->被调用函数入口->返回调用点。
	在连接过程中.plt表和.got表起着重要的作用。两个表中的PLT0和GOT0起着特殊的作用.
